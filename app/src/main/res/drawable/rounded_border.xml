<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Nền trắng -->
    <corners android:radius="16dp" /> <!-- Bo tròn góc -->
    <stroke
        android:width="2dp"
        android:color="@android:color/holo_blue_dark" /> <!-- Viền nổi bật -->
</shape>
