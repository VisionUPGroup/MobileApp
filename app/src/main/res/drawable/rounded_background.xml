<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/blue" /> <!-- Màu xanh biển nhạt trong suốt (#80 là alpha cho trong suốt) -->
    <corners android:radius="8dp"  /> <!-- Border radius 8dp -->
    <stroke android:width="1dp" android:color="@color/white"/>
    <padding
        android:left="8dp"
        android:right="8dp"
        android:top="4dp"
        android:bottom="4dp" />
</shape>
