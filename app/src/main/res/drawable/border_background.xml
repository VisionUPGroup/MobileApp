<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Màu nền -->
    <corners android:radius="8dp" /> <!-- Bo tròn góc (tùy chọn) -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp" /> <!-- Khoảng cách bên trong để tạo độ cách nhau -->
    <margin android:top="20dp"/>
    <stroke
        android:width="1dp"
        android:color="#D3D3D3" /> <!-- Đường viền -->
</shape>
