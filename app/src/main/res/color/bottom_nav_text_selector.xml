<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Màu khi mục được chọn -->
    <item android:state_checked="true" android:color="@color/blue" />
    <!-- Màu khi mục không được chọn -->
    <item android:color="@color/black" />
</selector>
