<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@drawable/background_notification"
    android:layout_marginBottom="8dp"
    android:elevation="2dp">

    <!-- Title -->
    <TextView
        android:id="@+id/notification_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textStyle="bold"
        android:textSize="16sp"
        android:textColor="#333333"
        android:text="Thông báo tiêu đề"
        android:ellipsize="end"
        android:maxLines="1" />

    <!-- Body -->
    <TextView
        android:id="@+id/notification_body"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:textSize="14sp"
        android:textColor="#555555"
        android:text="Đây là nội dung của thông báo, có thể dài hơn một chút để kiểm tra cách hiển thị." />

    <!-- Timestamp -->
    <TextView
        android:id="@+id/notification_timestamp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:textSize="12sp"
        android:textColor="@android:color/darker_gray"
        android:text="2 giờ trước" />

</LinearLayout>
